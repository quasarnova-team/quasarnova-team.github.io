# quasarnova-team.github.io

The quasarnova organization landing page — a single self-contained `index.html`,
served by GitHub Pages at https://quasarnova-team.github.io/.

No build step, no dependencies. Edit `index.html`, push to `main`, done.
